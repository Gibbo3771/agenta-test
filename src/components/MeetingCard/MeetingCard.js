// @flow

import React from "react";

export type Props = {
  children?: any
};

const MeetingCard = (props: Props) => {
  return (
    <div className="card">
      <span>ID</span>
    </div>
  );
};

export default MeetingCard;
